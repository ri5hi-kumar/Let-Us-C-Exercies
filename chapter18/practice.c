#include <stdio.h>
#include <string.h>
#include <ctype.h>
int main()
{
    /* int i = 12, date, month;
    float fl = 36.66;
    char ch = 'A';
    char s[10] = "i am don";
    printf("%-10f %-10f\n%-10c %-10s\n", i, fl, ch, s);
    printf("just checking the string\ntab\tbackspace \bcarrage\ralert\atest\n");
    //ignoring characters
    /*printf("entre in format of dd-mm\n");
    scanf("%d%*c%d\n", &date, &month);
    printf("%d %d\n", date, month);*/ 

    // int i;
    // char ch = 'a';
    // float fl = 3.14;
    // char str[20];
    // printf("%d %c\n", i, ch);
    // sprintf(str, "%d %c %f", i, ch, fl);
    // printf("%s\n", str);
    // return 0;

    // printf("More often than \b not  \rthe person who wins is the one who thinks he can!");
}